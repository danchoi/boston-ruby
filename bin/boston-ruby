#!/usr/bin/env ruby

command = ARGV.shift

case command
when /^m/
  require 'boston-ruby/mailing_list'
when /^t/
  require 'boston-ruby/twitter'
when /^b/
  require 'boston-ruby/blogs'
when /^g/
  require 'boston-ruby/github'
else
  abort "I don't recognized that command"
end
